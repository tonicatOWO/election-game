# LiveKit SFU config — development
# For production: replace key/secret and enable TLS
port: 7880
log_level: debug

rtc:
  tcp_port: 7881
  port_range_start: 7882
  port_range_end: 7890
  use_external_ip: false

keys:
  devkey: devsecret
